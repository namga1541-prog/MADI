# 마디 인계문서 — 2026-05-11 (오후 세션)

---

## 📌 오늘 한 줄 요약

Phase A 10가지 점검 완료 + Critical 수정 전부 완료 + Phase B 1순위(에러 추적) 구축 완료  
→ 다음 세션에서 **라이선스 시스템 클라이언트 코드 구축** 진행

---

## ✅ 오늘 완료한 작업 전체

### 1. 사전 작업
- **Edge Function ALLOWED_TABLES에 `madi_licenses` 추가 + Deploy** ✅

### 2. Phase A — 10가지 점검 (모두 완료)

| # | 항목 | 결과 |
|---|---|---|
| ① | JS 문법/런타임 (16개 파일) | 🟢 무결점 |
| ② | XSS 보안 | 🟢 양호 (1건 수정 완료) |
| ③ | 권한 체크 | 🟢 Edge Function이 잘 막음 |
| ④ | 에러 처리 (catch) | 🟢 매우 양호 (50/51) |
| ⑤ | null/undefined | 🟢 양호 (1건 수정 완료) |
| ⑥ | 모바일 반응형 | 🟢 매우 양호 (수정 불필요) |
| ⑦ | 이벤트 누수 | 🟢 무결점 |
| ⑧ | RLS 정책 | 🟢 양호 + 명시화 완료 |
| ⑨ | localStorage | 🟢 Critical 수정 완료 |
| ⑩ | 라이선스 우회 | 🟡 미구현 (다음 세션) |

### 3. 수정된 파일 목록

| 파일 | 변경 내용 | GitHub 반영 |
|---|---|---|
| **Edge Function /api** | cross-center 차단 (POST/PATCH/DELETE) 추가 + madi_error_logs 허용 | ✅ |
| **madi-01.js** | safeSetItem 헬퍼 추가 + 핵심 저장 함수 8곳 적용 | ✅ |
| **madi-03.js** | escHtml(e.message) XSS 보강 | ✅ |
| **madi-09.js** | portfolio null 가드 (`if (!pts || pts.length === 0) continue;`) | ✅ |
| **madi-02.js** | pushErrorLog에 Supabase 영구 저장 로직 추가 | ✅ |
| **admin.html** | 에러 로그 모니터링 카드 + loadErrorLogs/clearOldErrorLogs 함수 | ✅ |

### 4. Supabase 변경 사항

| 작업 | 상태 |
|---|---|
| madi_activities, madi_iep_history, madi_settings, madi_push_subscriptions — RLS service_only 정책 추가 | ✅ |
| madi_error_logs 테이블 생성 + RLS (service_only) + 인덱스 | ✅ |
| ALLOWED_TABLES에 madi_licenses, madi_error_logs 추가 | ✅ |

---

## ⚠️ 중요 발견 사항

### 라이선스 시스템 클라이언트 코드 미완성!

- **어제 인계문서**: "라이선스 시스템 통째로 추가 완료"
- **오늘 확인**: GitHub admin.html에 라이선스 관련 클라이언트 코드 **0건**
- **실제 상태**: Supabase madi_licenses 테이블 + RLS는 있음. 클라이언트 UI/JS는 없음

→ 다음 세션에서 라이선스 시스템을 **처음부터 새로 만들어야 함**

---

## 🎯 다음 세션 우선순위

### 🥇 1순위 — 라이선스 시스템 클라이언트 구축 (2~3시간)

구현할 것:
1. **슈퍼어드민용 발급 UI** (admin.html settings 탭)
   - 센터 ID + 플랜 선택 + 입금액 + 메모 입력
   - 6자리 키 자동 생성 (혼동 문자 I, O, 0, 1 제외)
   - 발급 이력 테이블
2. **센터장용 활성화 UI** (admin.html service 탭)
   - 현재 플랜 + 만료일 표시
   - 만료 D-7 이내 경고 배지
   - 6자리 키 입력 → 활성화 버튼
3. **JS 함수**: generateLicenseKey, calcExpiresAt, issueLicense, copyLicenseKey, loadLicenseList, loadMyLicense, activateLicense

### 🥈 2순위 — 라이선스 만료 서버 검증 (1시간)

결정된 정책: **강력 — 만료 시 로그인만 가능**

Edge Function에 추가할 로직:
```
로그인 요청 → 통과 (토큰 발급)
그 외 모든 요청 → madi_licenses에서 만료 여부 확인 → 만료면 403
```

### 🥉 3순위 — Phase B 2순위 (데이터 자동 백업)

- Supabase pg_cron + Edge Function backup-daily
- 매일 새벽 madi_* 테이블 전체 JSON 내보내기
- 사고 발생 시 복구 가능

---

## 🔑 라이선스 시스템 설계 (이미 결정된 것들)

### 키 생성 규칙
- 6자리 영숫자
- 혼동 문자 제외 문자셋: `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (I, O, 0, 1 제외)

### 플랜 구조
| 플랜 | 기간 | 가격 |
|---|---|---|
| trial | 30일 | 0원 |
| starter | 1개월 | 30,000원 |
| pro | 1개월 | 70,000원 |
| enterprise | 1개월 | 150,000원~ |

### 만료 정책
**강력**: 만료 시 로그인만 가능, 나머지 모든 API 요청 차단

### madi_licenses 테이블 스키마 (Supabase에 이미 존재)
→ 다음 세션 시작 시 `SELECT * FROM madi_licenses LIMIT 1;` 로 정확한 컬럼 확인 후 작업

---

## 📂 파일 상태 (GitHub 현재)

| 파일 | 오늘 변경 | GitHub 반영 |
|---|---|---|
| admin.html | 에러 로그 카드 + JS 추가 | ✅ |
| madi-01.js | safeSetItem 추가 | ✅ |
| madi-02.js | pushErrorLog Supabase 저장 | ✅ |
| madi-03.js | XSS 보강 1줄 | ✅ |
| madi-09.js | null guard 1줄 | ✅ |

---

## 🐞 에러 추적 시스템 사용법

### 에러 보기 (슈퍼어드민)
1. admin.html → 관리자설정 탭 (슈퍼어드민만)
2. 하단 🐞 오류 모니터링 카드
3. 센터별 필터 + 건수 선택 + 새로고침

### Supabase 직접 조회 (SQL Editor)
```sql
SELECT created_at, center_id, username, message, source
FROM madi_error_logs
ORDER BY created_at DESC
LIMIT 50;
```

### 30일 이상 로그 삭제
- 에러 로그 카드의 "🗑️ 30일+ 삭제" 버튼

---

## 💬 새 세션 첫 멘트

```
"클로드, 인계문서 봤어. 오늘은 라이선스 시스템 클라이언트 코드부터 시작할게."
```

먼저 할 것:
```sql
-- Supabase SQL Editor에서 실행 (madi_licenses 스키마 확인)
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'madi_licenses' 
ORDER BY ordinal_position;
```

이 결과를 클로드에게 보여주면 바로 코드 작업 시작 가능.

---

## ⚠️ 잊지 말 것

1. **라이선스 시스템은 처음부터 만들어야 함** — 어제 인계문서의 "완료" 표시는 잘못됨
2. **에러 추적 시스템 동작 확인** — admin.html 관리자설정 탭에서 🐞 카드 보이는지 확인
3. **인계문서 정확성 원칙** — 앞으로 인계문서에 "완료"는 "파일 저장 + GitHub 반영 + URL 동작 확인"까지 마친 것만 기재

---

**작성일:** 2026-05-11 (오후)  
**다음 세션:** 라이선스 시스템 클라이언트 코드 구축  
**상태:** Phase A 완료 + Phase B 1순위 완료 + 라이선스 시스템 미완성 확인됨
