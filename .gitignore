node_modules/
.env
playwright-report/
test-results/
test-results*.txt

# 현업 치료사 실제 보고서 원본 — 개인정보 포함, 로컬 보관만 (GitHub 푸시 금지)
.local-reports/
.local-reports-tmp/
.local-functions-backup-*/

# Claude Code 세션·워크트리·로컬 설정 — 로컬 전용
.claude/sessions-extract-*/
.claude/worktrees/
.claude/settings.local.json
.claude/snapshot-*.md

# Supabase CLI 임시 파일
supabase/.temp/

# 일회성 마이그레이션·리팩토링 스크립트 (실행 후 보관 의미 없음)
.local-scripts/

# 일회성 데이터 업로드 스크립트 (PII 포함, 콘솔 실행용 IIFE)
# 이름 규칙: *_upload_script*.js — 루트에 임시로 생기는 변종까지 자동 보호
*_upload_script*.js

# 사적 메모·인계 문서 (로컬 보관)
마디_인계문서_*.md

# 스토어 출시 시 사업자 정보·계정 비밀번호 등 비공개 자료 (로컬·비밀번호 매니저에만 보관)
store-assets/private/

# AI 코드 리뷰용 번들 — .local-scripts/build-review-bundle.js 로 언제든 재생성 가능
review-bundle/
