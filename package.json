{
  "name": "madi-app-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "lint": "eslint \"madi-*.js\" --max-warnings=0",
    "lint:fix": "eslint \"madi-*.js\" --fix",
    "check": "eslint \"madi-*.js\" --max-warnings=0 && node tests/smoke.js && node tools/check-patterns.js && node tools/check-sw-sync.js",
    "funcindex": "node tools/gen-functions.js",
    "setup": "git config core.hooksPath .githooks && node tools/gen-functions.js && echo setup done: hooksPath=.githooks",
    "deploy:fn": "node tools/deploy-fn.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "eslint": "^8.57.0",
    "eslint-plugin-no-unsanitized": "4.0.2"
  }
}
