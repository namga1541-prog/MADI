# 정기점검 체크리스트

> 매월 1일 / 매분기 첫 주에 실시. 결과는 분기별 보고서로 종합.

---

## 📅 월간 점검 (매월 1일, 약 30분)

### 1. 계정 위생
- [ ] 30일 이상 미접속 계정 목록 추출 → 비활성 검토
  ```sql
  SELECT username, role, last_seen_at
  FROM madi_users
  WHERE last_seen_at < now() - interval '30 days';
  ```
- [ ] 퇴사자·이직자 계정 status='inactive' 처리
- [ ] superadmin/admin 권한 보유자 명단 검토 (의도된 인원 일치 여부)
- [ ] 2FA(totp_enabled) 미설정 admin 계정 → 활성화 독려 이메일

### 2. 보안 알람·로그
- [ ] madi_audit_log 에서 madi_settings SELECT 빈도 확인 (이상 패턴?)
  ```sql
  SELECT actor_id, count(*)
  FROM madi_audit_log
  WHERE table_name = 'madi_settings' AND occurred_at > now() - interval '30 days'
  GROUP BY actor_id ORDER BY count(*) DESC LIMIT 10;
  ```
- [ ] 로그인 실패 5회 이상 누적 계정 (잠금 발생)
  ```sql
  SELECT username, failed_login_count, locked_until
  FROM madi_users
  WHERE failed_login_count > 0;
  ```
- [ ] Anthropic 사용량 (Console → Usage) — 전월 대비 +50% 이상이면 조사

### 3. 시스템 상태
- [ ] Supabase Dashboard → Database → Performance — 느린 쿼리
- [ ] Edge Functions → Logs — 5xx 에러율
- [ ] sw.js 캐시 버전 정상 갱신 여부 (pre-commit 훅 작동)
- [ ] GitHub Pages 빌드 실패 알림

### 4. 백업 검증
- [ ] Supabase 자동 백업 활성화 확인 (Dashboard → Database → Backups)
- [ ] 분기 1회 복구 테스트 (테스트 프로젝트에 실제 restore)

---

## 📆 분기 점검 (분기 첫 주, 약 2시간)

### 1. 권한 재인증
- [ ] 모든 admin·superadmin 권한 보유자에게 권한 유지 의향 확인 이메일
- [ ] 학부모 계정 — 자녀가 종결된 보호자 계정 정리

### 2. 비밀번호·자격증명 회전
- [ ] JWT_SECRET 회전 검토 (연 1회는 필수, 분기는 권장)
  - 회전 시: 전 사용자 자동 로그아웃 (사전 공지)
- [ ] Anthropic API 키 회전
- [ ] 직원 계정 비번 변경 권장 알림

### 3. 위탁사 점검
- [ ] Supabase 보안 인증 (SOC 2, ISO 27001) 유효성
- [ ] Anthropic Trust Center 변경사항 확인
- [ ] GitHub 보안 업데이트 (Dependabot Alerts) 검토

### 4. 모의훈련
- [ ] 분기 1회 침해사고 시나리오 훈련 (compliance/03 매뉴얼)
- [ ] 결과 보고서 → 매뉴얼 개정

### 5. 개인정보 정책 점검
- [ ] privacy.html 최신성 — 새 기능·새 위탁사 추가 시 갱신
- [ ] 만 14세 미만 동의서 누락 케이스 검토
- [ ] 종결 아동 데이터 보관 30일 정책 — 자동 파기 cron 정상 작동?
  ```sql
  SELECT count(*) FROM madi_children
  WHERE status = '종결' AND closed_at < now() - interval '30 days';
  -- 0 이어야 함 (cron 동작 시)
  ```

### 6. 종속성 보안
- [ ] CDN 라이브러리 (Chart.js, xlsx, supabase-js) 신규 CVE 검색
- [ ] `npm audit` (dev 종속성 — Playwright 등)
- [ ] Subresource Integrity 해시 검증 (변경 없으면 정상)

### 7. 데이터 무결성
- [ ] madi_audit_log 행 수 — 갑작스런 감소 = 삭제 시도 의심
- [ ] 비정상 INSERT/DELETE 패턴 (1초 내 100건 등)
- [ ] Storage 사용량·파일 수 추이

---

## 📊 연간 점검 (매년 1월, 약 1일)

### 1. PIA 갱신
- compliance/01-PIA 전면 재작성
- 신규 위험·기능 반영

### 2. 외부 침투 테스트
- 외부 업체 의뢰 (예산: 500만원~)
- 보고서 검토 → 후속 조치 계획

### 3. 직원 교육
- compliance/05-직원교육 자료로 전 직원 1회 (출석부 보관)
- 신규 입사자 별도 진행

### 4. 정책·약관 갱신
- privacy.html · 이용약관 · DPA 모두 검토
- 법령 변경 반영

### 5. 사이버 보험 갱신
- 보장 범위·한도 검토
- 사고 통계 기반 한도 조정

---

## 보고서 양식

```
[월간 점검 보고서 — 2026년 __월]
점검자: ____   점검일: ____   소요 시간: __분

1. 발견 사항: __건
2. 즉시 조치 필요: __건  ← 상세
3. 모니터링 대상: __건  ← 다음 점검까지 추적
4. 정상 항목: __건

상세:
- [HIGH] ____________  → 조치: ____
- [MED]  ____________  → 모니터링
- [LOW]  ____________  → 정상

다음 점검일: 2026-__-__
```

---

## 개정 이력

| 버전 | 일자 | 변경 사항 |
|------|------|----------|
| v1.0 | 2026-__-__ | 최초 작성 |
