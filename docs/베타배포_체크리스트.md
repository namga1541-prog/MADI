# 아이마디 베타 배포 체크리스트

> 베타 대상: 두목님(한기성) 센터 1곳  
> 배포 URL: https://namga1541-prog.github.io/MADI

---

## 1단계 — 서버 설정 확인 (대장님)

- [ ] Supabase Edge Functions 6개 모두 정상 배포 확인
  - ai-proxy / api / login / change-password / parent-auth / send-push
- [ ] ai-proxy에 quota 코드 교체 완료 (오늘 작업)
- [ ] `madi_ai_quota` 테이블 생성 SQL 실행 (아래 SQL 참조)
- [ ] 환경변수 `AI_DAILY_LIMIT` 설정 여부 확인 (기본 100회)

```sql
CREATE TABLE IF NOT EXISTS madi_ai_quota (
  user_id       TEXT NOT NULL,
  date          DATE NOT NULL DEFAULT CURRENT_DATE,
  center_id     TEXT,
  request_count INT  DEFAULT 0,
  PRIMARY KEY (user_id, date)
);
```

---

## 2단계 — 두목님 센터 데이터 점검 (대장님 + 두목님)

- [ ] 센터 정보 정상 입력 (센터명, 초대코드)
- [ ] 치료사 계정 생성 완료 (두목님 본인 + 소속 치료사)
- [ ] 아동 데이터 최소 3명 이상 입력
- [ ] 아동별 일정 1주 이상 등록
- [ ] 세션 기록 2~3건 작성 (AI 기능 테스트용)

---

## 3단계 — 학부모 계정 테스트

- [ ] 학부모 계정 1개 생성 (Supabase Dashboard → madi_users에서 직접 role: 'parent'로 생성)
- [ ] `madi_parent_children` 테이블에 학부모-아동 연결 행 추가
- [ ] 학부모 계정으로 로그인 → 홈/일정/리포트/공지 정상 표시 확인
- [ ] 학부모 계정으로 비밀번호 변경 테스트
- [ ] 학부모 화면에서 마로(AI) 버튼 미표시 확인

---

## 4단계 — 기능별 테스트 시나리오

### 치료사 시나리오
- [ ] 로그인 → 대시보드 정상 표시
- [ ] 아동 카드 클릭 → 세션 기록 작성
- [ ] AI IEP 생성 버튼 클릭 → 정상 동작
- [ ] 일정 등록 → 캘린더 표시 확인
- [ ] 마로(AI 챗봇) 질문 → 정상 응답

### 학부모 시나리오
- [ ] 로그인 → 홈 화면 정상 표시 (연결된 아동 정보)
- [ ] 일정 탭 → 내 아이 일정만 표시 확인
- [ ] 리포트 탭 → 내 아이 세션 기록만 표시 확인
- [ ] 공지 탭 → 센터 공지 표시 확인
- [ ] 알림 카드 → 미읽은 알림 표시/클릭 확인

### 보안 시나리오
- [ ] 학부모가 다른 아이 데이터 조회 불가 확인 (F12 → Network 직접 API 호출 테스트)
- [ ] 잘못된 비밀번호 5회 입력 → 잠금 동작 확인
- [ ] 로그아웃 후 토큰 만료 테스트 (7일 후)

---

## 5단계 — 모바일 점검

- [ ] iPhone Safari에서 전체 기능 확인
- [ ] Android Chrome에서 전체 기능 확인
- [ ] 학부모 탭바(모바일) 정상 표시
- [ ] 학부모 사이드바(PC) 정상 표시
- [ ] 터치 버튼 크기 적절 (최소 44px)

---

## 6단계 — 배포 전 최종 확인

- [ ] 오류 모니터링 대시보드에서 기존 오류 30일+ 삭제
- [ ] API 사용량 초기화 (설정 탭 → 디버그 → 사용량 초기화)
- [ ] 두목님에게 초대코드 전달
- [ ] 두목님에게 학부모 앱 사용 안내서 전달

---

## 알려진 제한사항 (베타 기간 중 허용)

| 항목 | 현황 | 예정 |
|------|------|------|
| 토큰 폐기 | 로그아웃 후 7일간 유효 | v1.1 |
| /api rate-limit | 없음 | v1.1 |
| 학부모 마로 | 비활성화 | v1.2 (학부모용 별도 구현) |
| 결제 시스템 | 미구현 | v2.0 |

---

*작성일: 2026-05-17*
