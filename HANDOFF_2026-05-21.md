# 마디 인계 노트 — 2026-05-21 (회사 PC 재개용)

> **새 Claude Code 세션 시작 시 이 문서를 그대로 첨부해 주세요. 그러면 빠르게 컨텍스트 회복합니다.**

---

## 0. 빠른 재개 (회사 PC)

```powershell
# 환경 이미 있으면
cd C:\path\to\madi-app
git pull

# 처음 셋업이면
git clone https://github.com/namga1541-prog/MADI.git
cd MADI
scoop install supabase
supabase login
supabase link --project-ref ujxdhafzjyrglaclarwe
```

마지막 커밋: `2484830 chore(ui): 헤더 사용자 드롭다운에서 '비밀번호 변경' 메뉴 항목 제거`

---

## 1. 어제 (2026-05-20) 한 작업 한눈에

### 보안 강화 5개 트랙, 총 44건 결함 처리

| 트랙 | 처리 건수 | 핵심 변경 |
|------|-----------|----------|
| 함수 오류 QA | 22건 (CRITICAL 4 / HIGH 7 / MEDIUM 6 / LOW 5) | 학부모 격리 쿼리, MIME 검증, JWT alg, undefined.trim, async→.then 등 |
| Second-pass | 3건 | escHtml `'` escape, 다자녀 셀렉터, assessments.user_id 컬럼 |
| 학부모 데이터 격리 | RLS 9개 정책 재작성 + 2개 누수 제거 | `madi_my_child_ids()` 헬퍼, 자녀별 격리, 라운지 SELECT 차단 |
| 보안 종합 (C1+H1~H6) | 7건 (H4 비번 정책은 롤백) | Storage anon 차단, logout PII 정리, 에러 sanitize, admin.html 서버 검증, CSP, SRI |
| 시니어 관점 후속 | 4건 | madi_audit_log + 21 트리거, /api JWT alg/role escalation 차단, localStorage cn3_* 폐지 |
| 추천 후속 | 3건 | 세션 무효화(password_changed_at), prompt injection 가드, 일일 다이제스트 |

### 마지막 UI 정리
- 헤더 사용자 드롭다운에서 "비밀번호 변경" 메뉴 제거 (로그아웃만 표시)
- `showChangePasswordModal()` 함수는 유지 — admin.html 등 다른 진입점 대비

---

## 2. 현재 라이브 상태

### Git
- `origin/main = 2484830`, 작업 트리 깨끗
- untracked 는 의도적 미커밋 (`.claude/`, `supabase/.temp/`, 사적 메모 등)

### DB 마이그레이션 (모두 적용 완료)
1. `author_id_migration.sql`
2. `author_name_enforce_trigger.sql`
3. `notices_author_id_migration.sql`
4. `rate_limits_setup.sql`
5. `assessments_user_id_migration.sql`
6. `parent_isolation_rls.sql`
7. `storage_policies_tighten.sql`
8. `audit_log_setup.sql`
9. `session_invalidation_setup.sql`
10. `daily_digest_setup.sql`

### Edge Functions (모두 최신 버전 라이브)
| 함수 | 버전 | 핵심 보강 |
|------|------|-----------|
| login | v24 | JWT iat 추가, bcrypt lazy migration |
| api | v36 | path 화이트리스트, JWT alg, iat 검증, madi_users select 정제, role escalation 차단 |
| ai-proxy | v16 | prompt injection 가드 (모델 화이트리스트, content 한도, 안전 가드 prepend) |
| change-password | v6 | bcrypt(cost 12), password_changed_at 동시 갱신 |
| upload-image | v9 | MIME magic-byte, JWT alg, 에러 메시지 일반화 |
| parent-auth | v7 | DB 기반 rate limit, 비번 4자 (롤백 상태) |
| send-push | v7 | (변경 없음) |
| github-deploy | v4 | (변경 없음) |
| logout | v2 | (변경 없음) |

### 자동화
- `pg_cron` 'madi-daily-digest' 매일 09:00 UTC (KST 18:00) 실행
  - 슈퍼어드민 전원에게 24h 활동 요약 인앱 알림
- `madi_audit_log` 7개 테이블 × 3 이벤트 = 21개 트리거 작동 중

---

## 3. 알려진 사용자 영향

⚠️ **모든 활성 사용자 1회 재로그인 필요** — `password_changed_at` 마이그레이션 시점이 모든 row 의 기본값으로 들어가서, 그 이전 발급된 JWT 는 api Edge Function 에서 401 응답
⚠️ **Ctrl+Shift+R 강제 새로고침 1회** — CSP 메타·SRI 적용, Service Worker v4-20260520-2239
✅ **테스트용 비밀번호 1234 유지** — H4 롤백 상태. 운영 전환 시 정책 강화 필요 (이력 `fc9febe`)

---

## 4. 다음 후보 작업

### 운영 전환 전 필수 (시간 잡고 작업)
- [ ] **JWT 24h → 1~2h + Refresh Token** 흐름 — 토큰 도난 시 노출 시간 단축. 작업 상.
- [ ] **비밀번호 정책 복원** (4자 → 8자+영숫자) — 운영 전환 D-day
- [ ] **백업·복구 dry-run 절차서** — Supabase 자체 백업 검증 + 복구 시나리오

### 가성비 높은 항목 (1~2시간)
- [ ] **개인정보 처리방침 명시적 동의** — 가입 폼 체크박스 + `madi_users.consent_at` 컬럼. PIPA 컴플라이언스
- [ ] **로그인 실패 알림** — 새 IP/기기 감지 시 본인·슈퍼어드민에게 다이제스트 첨부
- [ ] **운영 console.warn/error 정리** — `if(MADI_DEBUG)` 가드로 wrap

### 보류 (유료 사용자 100명+ 되면 검토)
- [ ] 관리자 2FA (TOTP) — 등록·관리 부담 큼
- [ ] 비밀번호 재설정 (이메일/SMS) — `madi_users.email` 컬럼 없음, SMS 인프라 필요

---

## 5. 새 Claude Code 세션 시작 첫 메시지 템플릿

```
마디 앱 작업 이어갑니다. 인계 문서 HANDOFF_2026-05-21.md 참고.
오늘 작업할 항목: [여기에 4번 섹션 중 골라서 적기]
```

---

## 6. 빠른 참조

### 프로젝트 식별자
- GitHub: `https://github.com/namga1541-prog/MADI`
- Supabase project ref: `ujxdhafzjyrglaclarwe`
- 운영 URL: `https://namga1541-prog.github.io/MADI/`

### 자주 쓰는 명령
```powershell
# 코드 받기·올리기
git pull
git push origin main          # pre-commit 훅이 SW 캐시 자동 갱신, post-commit 훅이 자동 push

# DB 마이그레이션 실행
supabase db query --linked --file <파일>.sql

# DB 임시 쿼리
supabase db query --linked --output table "SELECT ..."

# Edge Function 배포 (반드시 --no-verify-jwt)
supabase functions deploy <함수명> --project-ref ujxdhafzjyrglaclarwe --no-verify-jwt

# Edge Function 소스 다운로드 (로컬에 없을 때)
supabase functions download <함수명> --project-ref ujxdhafzjyrglaclarwe
```

### CLAUDE.md 핵심 규칙
- 바닐라 JS (ES5 호환: `var`, `function`, `.then()`)
- 사용자 입력은 반드시 `escHtml()` 처리
- DB 접근은 `supaFetch()` 경유, 직접 anon key 사용 금지
- 모든 코드 수정 후 자동 커밋·푸시 (사용자 명시 거부 시 외)
- Edge Function 배포 시 `--no-verify-jwt` 필수
